[project]
name = "home-assistant-hakera"
version = "0.4.3"
description = "Home Assistant integration for Makera Z1 monitoring and accessory control"
readme = "README.md"
requires-python = ">=3.14.2"
license = "MIT"

[project.urls]
Documentation = "https://github.com/mattriney/home-assistant-hakera"
Issues = "https://github.com/mattriney/home-assistant-hakera/issues"
Source = "https://github.com/mattriney/home-assistant-hakera"

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["B", "C4", "E", "F", "I", "N", "UP", "W"]

[tool.pyright]
include = ["custom_components/hakera", "tools"]
extraPaths = ["custom_components/hakera"]
pythonVersion = "3.14"
typeCheckingMode = "basic"

[tool.unittest]
start-directory = "tests"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
